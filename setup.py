#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='spasne',
      version='1.0',
      description='Spatially resolved t-SNE',
      url='https://github.com/Lin-Xu-lab/SpaSNE.git',
      packages=['.'],
     )
